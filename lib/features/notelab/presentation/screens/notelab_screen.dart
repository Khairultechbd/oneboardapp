import 'package:flutter/material.dart';

class NoteLabScreen extends StatelessWidget {
  const NoteLabScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('NoteLab'),
      ),
      body: const Center(
        child: Text('NoteLab Screen - Coming Soon'),
      ),
    );
  }
} 